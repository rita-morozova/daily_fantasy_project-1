<h1><%=image_tag('football-banner')%></h1>
<h1><%="Contest: " + @contest.name %></h1>
<h2><%="Budget $ " + @contest.budget.to_s %></h2>
<h2><%="Prize $ " + @contest.prize.to_s %></h2>
<%= link_to "Join Contest", new_team_path %>
<ol>

    <% @contest.ranked_users.each do |user|%>
        <% if user.id == session[:user_id] %>
        <li><%= link_to user.username, team_path(user.teams.where(contest_id: @contest.id)[0].id)%><%=  " " + user.score_for_contest(@contest).to_s %></li>
        <% else %>
        <li><%= user.username + " " + user.score_for_contest(@contest).to_s %></li>
    <% end %>
    <% end %>
<ol>
